[project]
name = "git-explorer"
version = "0.1.0"
description = "Interactive TUI for exploring GitHub repository history"
requires-python = ">=3.11"
dependencies = [
    "textual>=0.70.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
